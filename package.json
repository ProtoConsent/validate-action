{
  "name": "protoconsent-validate",
  "version": "1.0.0",
  "description": "GitHub Action and CLI to validate .well-known/protoconsent.json files",
  "main": "src/index.js",
  "bin": {
    "protoconsent-validate": "./src/cli.js"
  },
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "node --test test/validate-core.test.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
